const initialState: object = {
  message: "",
  from: "",
  to: "",
};

const clientReducer = (
  state: object = initialState,
  action: object
): object => {
  return state;
};

export { clientReducer };
