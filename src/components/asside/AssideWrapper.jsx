import styled from "styled-components";

const Wrapper = styled.div`
  display: grid;
  grid-area: asside;
  background: #aaa398;
`;

const AssideWrapper = () => {
  return <Wrapper></Wrapper>;
};

export default AssideWrapper;
